// each story will have IMAGE, but not sure how to incorporate that yet.

export class Story {
  constructor(public title: string, public author: string, public date: string, public article: string, public rating: number, public comment: string) { }
}
